const lighthouses = ['Gibraltar Point', "Peggy's Point", 'Cove Island', 'Discovery Island', 'Cape Scott', 'Point Clark', 'Kincardine']

const size = lighthouses.map(element => element.length)
console.log()
console.log(size)


// went through the array and counter letters for each entree
// with the use of .map callback